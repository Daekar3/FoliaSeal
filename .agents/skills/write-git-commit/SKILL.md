---
name: write-git-commit
description: Review changed files and determine appropriate groups for one or more git commits.  Stage files and write a git commit message for the given changes, then commit them. Use when user wants to create a commit message for their changes.
---

Review the changed files and determine appropriate groups for one or more git commits.  Changes should be grouped logically so that each commit represents a coherent set of changes.  Stage files and write a git commit message for the given changes, then commit them.